services:
  minio:
    image: minio/minio:latest
    container_name: mllab-minio
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    command: server /data --console-address ":9001"
    networks:
      - mllab-network

#  minio-init:
#    image: minio/mc:latest
#    container_name: mllab-minio-init
#    depends_on:
#      - minio
#    volumes:
#      - ../infrastructure/minio/create-buckets.sh:/create-buckets.sh
#    entrypoint: /bin/sh
#    command: /create-buckets.sh
#    networks:
#      - mllab-network